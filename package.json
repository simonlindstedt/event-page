{
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npx servor dist index.html 4000 --reload --browse",
    "build": "parcel build index.html",
    "dev": "parcel index.html --port 8000"
  },
  "devDependencies": {
    "parcel": "^2.0.0-beta.1",
    "sass": "^1.32.8"
  },
  "dependencies": {
    "animejs": "^3.2.1",
    "scrollmagic": "^2.0.8"
  }
}
